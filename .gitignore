# Xcode (XcodeGenで再生成するため)
*.xcodeproj/
xcuserdata/
*.xcworkspace/

# macOS
.DS_Store

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods
Pods/

# Build artifacts
build/
DerivedData/

# Supabase local temp (contains connection info)
supabase/.temp/

# Environment / Secrets
*.xcconfig
.env*

# Claude Code
.claude/settings.local.json
.claude/session-state.md
.claude/session-state.md.bak
HANDOFF.md
# .mcp.json is committed (contains no secrets, shared across team).
# For credential-backed MCPs, create .mcp.local.json instead:
.mcp.local.json

# Serena MCP tool cache
.serena/
