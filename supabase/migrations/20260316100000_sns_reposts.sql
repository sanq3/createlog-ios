-- Migration: Add repost & quote support to posts table
-- Creates posts table (if not exists) with repost_of_id, quote_of_id, repost_count
-- Adds triggers for automatic repost_count maintenance

-- =============================================================================
-- 1. posts テーブルのrepost/quote対応
-- テーブルは 20260315150000 で作成済み
-- カラム追加・インデックス・制約は 20260318082108 で適用
-- ここではスキップ
-- =============================================================================

-- =============================================================================
-- 3-4. トリガー（repost_count, updated_at）
-- repost_of_id カラムは 20260318082108 で追加されるため
-- トリガーもそちらで作成。ここではスキップ。
-- =============================================================================

-- =============================================================================
-- 5. RLS policies
-- 正しいポリシーは 20260315150005 で適用済み（visibility/block対応）
-- 不正な USING(true) ポリシーは作成しない
-- =============================================================================
